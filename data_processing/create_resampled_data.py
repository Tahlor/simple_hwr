# Load all data from jsons
# Create a dataloader with resampler
# save out with                 output.append({"stroke": p,
#                                "text":GT_DATA[name],
#                                "id": name,
#                                "distance": np.average(distance)
